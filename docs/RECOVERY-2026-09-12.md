# Recovery and implementation receipt — 2026-09-12

Initial snapshot: 13:41 UTC; subsequent observations are appended below. This is
an implementation receipt, not a new strategy result or exchange qualification.
Historical reports and frozen evidence remain intact. The latest dated update
takes precedence over the initial running-process snapshot.

## Readiness at this snapshot

| Area | Evidence-backed state |
| --- | --- |
| Windows environment | CPython 3.11.15 and uv 0.12.3 restored; older environments preserved |
| Signed delivery | GPG signatures verified locally and on GitHub; main only |
| Runtime data | Bounded PostgreSQL repair running; not caught up across five symbols |
| Forward research | Exclusive September 12 UTC boundary; integrity/recovery valid; performance sealed |
| Paid-provider guard | Cumulative campaign code tested/published; historical reconciliation and adoption pending |
| Complete technical integration | NOT_REVALIDATED for the unfinished multi-repository release |
| EVEDEX DEV | NOT_QUALIFIED: no accepted 24-hour window, canary or seven-day soak |
| Alpha / LIVE | false / false; automatic strategy off |
| Market-data paper simulator | Alternative approved in principle; not implemented by this receipt |

## Completed delivery

- Execution `cd2aa21563aa7f4a89e2d7df27e38a915e7eb208`: acknowledge canonical,
  correctly scoped Risk refusals without recovery, trade locks or venue calls.
  Approved-entry guards are preserved.
- Execution `7328583b998c28e0baa96f88115bb5b1ea56479e`: integration fixtures
  require an explicitly confirmed disposable database and verify the actual
  server database before writes. Local: 302 tests; real Docker guard/journal/
  PAPER integration: 119 tests. All six GitHub CI jobs passed.
- Quant `230522dface6e73689ab5f836e78f0f632630884`: recovery phase, progress,
  confirmed-prefix and failure diagnostics. An uncertain publish remains
  UNKNOWN, not an automatic retry. 183 local tests and GitHub CI passed.
- Persistence `0b98d0d` fixes the old formatting gate;
  `9e7213a38f9a3d42f211d75e032e85a91cc4a662` adds cumulative provider budgets,
  receipt-bound adoption and guarded database targeting. 86 focused tests and
  GitHub CI passed. A separate real PostgreSQL drill confirmed concurrent
  admission, historical reservations, restart/month rollover and immutable
  adoption. No paid calls or primary migrations were used in the drill.
- LLM `03ab2f292045c542e97cc30bbe69f20f36bea98d`: mechanics probes use the
  same ledger; 62 tests, local quality/build and GitHub CI passed.
- Text `015798e928a4a15de822884fdcbb8f4870c6df2f`: X runtime/probes share
  the cumulative campaign; 89 tests, local quality/build and GitHub CI passed.
- Deploy `999b519bcfda7343f9c60f3d6775032ef95570c8`: reviewed Quant/Execution
  pins in both manifests/Compose files. Remote validation, 59 local tests and
  complete GitHub image-build CI passed.
- Deploy `4fc563c`: explicit ChatGPT/X import labels; generic EVEDEX keys are
  never inferred to be DEV. Nine provisioning tests passed. `b1117cd` also
  corrects secret ACL recovery: protected files require explicit file grants,
  not merely an inheritable directory grant. Existing bytes were not replaced.

The initial eight-repository Windows gate passed all 56 lock/quality/test/build
checks before subsequent feature edits. It does not replace a final gate of the
completed release. Frozen backtest dependency locks were not refreshed.

## Forward recovery is complete, not continuous uptime

The existing official supervisor ran from 13:12:59 to 13:19:25 UTC on September
12. It retrospectively retrieved September 7–11: 25 official daily archives,
36,000 new bars, zero duplicates. The verified ledger now contains 367,200 bars
(73,440 per symbol), exclusive boundary `2026-09-12T00:00:00Z`.

- Evidence SHA-256:
  `5464c9ac23bc9c1ed981da5fd7169efe1ffbc5ce804e6e413b038b13ae359440`.
- Identical unique post-sync backup/recovered SHA-256:
  `ddd26f7883f17fd94fa93f0d68e0f82231800c98a4cb3fffd6c210ad9e0a608e`.
- Primary unchanged during drill; eleven complete blind days; duration gate
  false; closed-trade count not evaluated; no PnL disclosed.
- Backtest `reports/regime-aligned-forward/WARMUP.md` records the paths and
  hashes, commit `9ed9d6a018d8620a82b694d3b4e0992957176809`; CI passed.

Trial 15 still requires both 365 complete forward days and 500 closed simulated
trades per scenario. Quarter-hour V2 remains blocked at 79/335 by the documented
official XRP archive conflict. Do not retry that source, skip a batch, substitute
daily data or open the conditional overlay without its parent gate.

## Bounded runtime recovery — initial 13:41 UTC observation

This is separate from the SQLite forward ledger above.

- Project `kairos-paper-gate`; existing volumes preserved.
- Fresh PostgreSQL backup: 38,571,328 bytes; SHA-256
  `afcd1617b38e0ea65268d23b29a3e4074be11a037422de341f3b35ad0691fcaf`.
- Restore passed: 12 migrations, 18 critical tables. Full canonical prefix
  validation passed; no duplicate/internal missing minutes in retained history.
- Run `20260912T133116Z-cefdd6d8`; PID 38588, start
  `2026-09-12T13:31:16.5225687Z`; container
  `kairos-gap-recovery-b10da46a98a14b6cb8f499cc9b655573`.
- Fixed end-exclusive `1789218000000` (`2026-09-12T13:00:00Z`);
  required increment 88,144 bars, maximum 150,000.
- At 13:41:21 UTC: 4,975 committed bars, no reported failure; not complete.
- Receipt/log prefix:
  `D:\Kairos\runtime\long-gap-recovery-20260912T133116Z-cefdd6d8`.

Do not duplicate a live process/container. Strategy, Risk and Execution remain
stopped. After actual completion verify all-symbol common watermark, canonical
continuity and outbox consistency. Any further tail beyond ordinary catch-up
bounds requires a separately bounded, backed-up continuation, not cursor resets.
Backfill is not evidence of a 24-hour observation window.

## Secrets and budget

The old shadow secrets are retained. Newly supplied provider values differ and
were imported into a separate access-restricted local directory. The original
user file was neither modified nor mounted. No credential value enters Git.

OpenAI $12, DeepSeek $1 and X $2 remain cumulative qualification ceilings,
including previous committed spend and uncertain reservations. The intended
authority is the existing paid-shadow database, not PAPER or test/restore copies.
Historical reconciliation must finish before adoption or further paid probes;
no fabricated zero-history registration was performed.

## Clarified paper route

The user permits an alternative if EVEDEX does not provide a usable paper venue.
The [official SDK](https://github.com/evedex-official/exchange-bot-sdk/blob/master/README.md)
distinguishes API-key account reads from wallet-signed order mutations. General
exchange/partner permissions do not establish dedicated DEV signing authority.
The [official profiles](https://github.com/evedex-official/exchange-bot-sdk/blob/master/src/params.ts)
still separate DEV, DEMO and PROD. A generic key is not relabelled as DEV.

Two claims remain independent:

1. **Market-data simulated paper** must use real causal data with conservative
   fees, book depth, slippage, latency, partial fills, SL/TP/timeout and restart.
   It needs a separate simulated namespace and stale-data rejection. It is not
   the old synthetic immediate-fill dry run and cannot prove EVEDEX execution.
2. **EVEDEX DEV qualification** keeps its own credentials, five-instrument
   quality gate, actual 24 hours, bounded canary and seven-day soak. Until these
   pass, EVEDEX `PAPER_QUALIFIED` remains false regardless of simulator results.

Neither route enables LIVE, real funds or an unapproved automatic strategy.

## Remaining work

- Complete bounded canary storage and independent Execution dispatch barrier,
  runner, acceptance and fault/race integration.
- Integrate Macro causal history and exact strategy-ID allocations; reject
  mixed account/environment histories and unknown/empty strategy lists.
- Deliver a coherent immutable analytical shadow stack; synchronize service
  SHA/lock pins and run final Windows, Docker and CI gates.
- Reconcile historical API spend, roll out credentials and perform only the
  still-missing bounded provider checks.
- Finish runtime catch-up and measure actual uptime/reconnect/recovery.
- Implement the distinct simulated-paper route if DEV remains unusable;
  preserve the separate real-exchange qualification blocker.
- Continue strategy work without altering frozen evidence or claiming alpha
  from infrastructure/model capability. Graphical decision monitoring remains
  a later delivery before user-facing paper trials, not part of this receipt.

## Subsequent observations — 13:52–13:58 UTC

The runtime repair stopped, rather than completing:

- Run `20260912T133116Z-cefdd6d8` reported `FAILED`, phase `DURABLE_PUBLISH`,
  error `TimeoutError`. `2026-09-12T13:52:14.927587Z` was its last-progress
  timestamp, not the time of failure emission; the log's last write was 13:53:04.
- The unresolved operation is ETHUSDT minute `[1788445440000, 1788445500000)`.
  Its outcome remains `UNKNOWN`: a timeout is not proof of rollback or of a
  network cause. No automatic retry or cursor reset was performed.
- The last confirmed increment is 6,487 bars. BTC reached the fixed exclusive
  boundary `1789218000000`; ETH's confirmed prefix is `1788445440000`;
  SOL/BNB/XRP remain at `1787842380000`. This is not five-symbol catch-up.
- At 13:57 UTC the Windows supervisor PID was absent. PostgreSQL bar/outbox
  reconciliation and Docker responsiveness are under read-only investigation.
  New entries and original trading consumers remain stopped.

Macro causal history restoration is now published as
`5b2e162f241f07bd854946f5ebd2f3b918bfe08c`. It restores bounded, read-only event
history without model calls, validates account/environment/time consistency and
permits only exact configured strategy IDs. An empty list means full reserve,
not inferred trading permission. Windows: 78 passed, one SQL test skipped;
the real PostgreSQL history test passed separately. Local quality/build and all
GitHub CI jobs passed. Full-stack revision integration remains outstanding.

Historical paid-provider reconciliation is **BLOCKED_NOT_ADOPTED**. Read-only
database/corpus matching found 20 existing reservations, plus six earlier
successful inference probes outside that ledger. Per-call recorded estimates
and unresolved reservations total $0.182102 for OpenAI and $0.001305 for
DeepSeek; these are not provider invoices. No cumulative campaign was adopted
and no new paid calls were issued.

A separate authenticated, read-only X Console observation found two requests
on August 18 and eleven billable events. Its usage screen shows $0.07 while
billing shows $0.06 current spend and $9.94 balance. Both values are preserved;
the discrepancy is not silently rounded away. There is no August 26 row in
the displayed period, but the historical token-to-account relationship and the
unknown timeout charge remain unproven. This does not justify clearing the
reservation. Private account identifiers remain only in local evidence.

Local reconciliation receipts are under
`D:\Kairos\runtime\expense-reconciliation-20260912T135500Z`:

- Original report JSON SHA-256:
  `5494310d46594457befc0921f2931e219d780119c96312c9f052074f55ee6813`.
- Append-only X Console addendum JSON SHA-256:
  `86a2f4574e457d770a5f7bfec17f6922c40807b4a3bf27154696fde713bc7a3d`.

The simulator design is documented locally; model implementation/tests are
in progress. Neither a complete simulator nor DEV qualification is claimed.

## Authoritative diagnosis and one continuation — 14:06–14:26 UTC

Read-only reconciliation resolves the earlier uncertain publish as
**COMMITTED_NOT_YET_DISPATCHED**. The ETH minute has exactly one canonical
audit row and one matching outbox row; both SHA-256 checks passed. The old run
actually committed 6,488 bars, one beyond the 6,487 client-confirmed count.
All five retained prefixes are contiguous without duplicate minutes. No
producer or advisory lock from the old run remained.

The failure interval overlaps substantial local persistence stalls: PostgreSQL
checkpoint sync took 237.761 seconds (maximum single sync 104.799 seconds),
and a Redis background save took 186 seconds. These exceed the 30-second
command timeout. This localizes the incident to persistence responsiveness;
it does not prove the exact timed-out sub-operation or a particular host cause.
Later isolated PostgreSQL/Redis probes returned in less than one second.
No queue, cursor, durability setting or retained evidence was reset.

- Diagnosis: `D:\Kairos\runtime\gap-diagnostic-20260912T140000Z\verified-outcome.json`.
  SHA-256 `4758cde334067e80c5b394f2e8144456310ecdc70b3c8b6e7d6aa78a430ce6e3`.
- Quant `375a355ace20c747fd36a5d3fa09dcc1b618ee03` now emits an independent
  observation timestamp, distinct from last progress. 184 Windows tests and
  [CI](https://github.com/Kairos-cryptoAI/kairos-quant-scouts/actions/runs/34698193134)
  passed. Deploy `ae1f36d428f5b372bd81a6c63c63c7e213a02c23` pins that revision;
  60 local tests, manifest/preflight validation and image-build CI passed.
- A new unique PostgreSQL backup contains 40,468,040 bytes, SHA-256
  `ebf214545e133c7ab91a6ae22afeb63d6e2ddbb4f53d5793f5b42a772d3d7b16`.
  Restore verified 12 migrations, 18 critical tables and 19 checkpoints plus
  the public sequence. This did not migrate the primary database.
- One bounded continuation started at `2026-09-12T14:18:39.4544983Z`:
  run `20260912T141839Z-13b63d27`, Windows PID 34640, container
  `kairos-gap-recovery-5ef30f5eb71840ecbd0e4d4a459355dd`.
  The fixed exclusive end remains `1789218000000`; remaining increment at
  launch was 81,656 bars, below the 150,000 hard maximum.
- At 14:20:39 the process/image identity was confirmed; primary audit/outbox
  counts matched, no orders/trades/paid rows existed, and 12 migrations remained.
  At 14:26:29 the producer reported 4,179 newly committed bars; still running.
- Receipt directory: `D:\Kairos\runtime\long-gap-continuation-20260912T141300Z`.
  Log prefix: `D:\Kairos\runtime\long-gap-recovery-20260912T141839Z-13b63d27`.

This is not a retry loop. A new failure requires new authoritative diagnosis.
Completion requires five-symbol continuity **and actual outbox delivery**, not
just committed rows. Trading consumers remain stopped. Existing automation
monitors this exact process without duplicating it; its daily forward task is
separately limited to one new attempt per day. Normal waiting remains quiet.

## Published safety components — 14:26 UTC

- Persistence `730ff1a878305ffef80a07a615795208a0372091`: database-time,
  scope-bound 24-hour read-only acceptance receipts; one globally active,
  explicitly armed session with at most ten entry attempts and two hours;
  exact arm/review/decision/effect binding; a committed dispatch claim before
  the venue call; a stop/dispatch lock and no reissue of an uncertain effect.
  Windows: 139 unit tests. Six dedicated real PostgreSQL tests passed locally
  and ran successfully in the separate
  [CI gate](https://github.com/Kairos-cryptoAI/kairos-persistence/actions/runs/34698658751).
  These are synthetic qualification fixtures, not a real 24-hour DEV window.
  New migrations have not been applied to the primary data-recovery database.
- Risk `145c3b1deba381a966374867e43c734fdc5e7211`: bounded, single-step session
  runner with preview, explicit arming, status, stop and next-slot submission.
  Missing/expired/wrong-session admission cannot create an entry. No automatic
  loop or fabricated acceptance override exists. 199 Windows tests and
  [CI](https://github.com/Kairos-cryptoAI/kairos-risk-manager/actions/runs/34698824699)
  passed. Execution's independent final barrier still needs its own integration
  publication; this Risk change alone does not authorize a canary.
- Execution `82d91186b6345538e819f92bd180b9f73c7e0c29`: a **unit-tested
  simulation kernel**, not a functioning paper-trading service. It models
  causal IOC depth consumption, partial/no fills, adverse tick/slippage/fees,
  immutable replay and stale/gap/reconnect rejection. 103 focused Windows
  tests, quality checks and
  [CI](https://github.com/Kairos-cryptoAI/kairos-execution-engine/actions/runs/34699035956)
  passed. Durable market-data recording, concurrent state ownership, positions,
  SL/TP/timeout, funding/margin and runtime wiring remain unimplemented for this
  separate simulator; the existing EVEDEX lifecycle is not silently reused as
  proof of simulated lifecycle correctness.

The published revisions above do not yet constitute one integrated deployment.
Final dependency synchronization, full-stack Windows/Docker checks and actual
data/venue qualification remain pending. `PAPER_QUALIFIED`, `ALPHA_READY` and
`LIVE_READY` remain false; no paid API calls or exchange mutations were issued.

## Coherent source release and second recovery stop — 14:45 UTC

The independent Execution entry barrier is now published as
`641a08045c70f15ab9d7a5b04e54ed0928a71529`. Windows: 416 unit tests; isolated
Linux/PostgreSQL: one actual session/arm/journal-to-engine composition plus
42 lifecycle/journal tests. All six
[CI jobs](https://github.com/Kairos-cryptoAI/kairos-execution-engine/actions/runs/34699786496)
passed, including its standard PostgreSQL job. The input Risk decision in that
composition is still a fixture, not a real RiskService result. The detailed
[verification receipt](https://github.com/Kairos-cryptoAI/kairos-execution-engine/blob/641a08045c70f15ab9d7a5b04e54ed0928a71529/docs/BOUNDED_ADMISSION_VERIFICATION_20260912.md)
records this boundary explicitly.

Runtime Git dependencies are synchronized to Persistence
`730ff1a878305ffef80a07a615795208a0372091` and, where used, LLM
`9e0e9e825963e9285fcbf1c416de93850ce24df1`. All changes below are signed on
`main`; dependency-only updates do not alter generators or other package versions.

| Runtime repository | Published revision | Windows unit tests | GitHub CI |
| --- | --- | ---: | --- |
| LLM | `9e0e9e825963e9285fcbf1c416de93850ce24df1` | 62 | [PASS](https://github.com/Kairos-cryptoAI/kairos-llm/actions/runs/34699714001) |
| Quant | `7349e72346324fd38b52683177e2eb601eea67da` | 184 | [PASS](https://github.com/Kairos-cryptoAI/kairos-quant-scouts/actions/runs/34699825251) |
| Strategy runtime | `8e24f518076f2f641409c4895879c7fb51183399` | 340 | [PASS](https://github.com/Kairos-cryptoAI/kairos-strategy-engine/actions/runs/34699774418) |
| Text | `855494dd851787a9f58e114b5ca765ca1eee6151` | 89 | [PASS](https://github.com/Kairos-cryptoAI/kairos-text-scouts/actions/runs/34699931686) |
| Router | `db8e85fda79e7a7a1472aed9fd6c0143c984080c` | 55 | [PASS](https://github.com/Kairos-cryptoAI/kairos-router/actions/runs/34699829257) |
| Aggregator | `7b158780ef439edc0b8b1054bcfff409892c0852` | 49 | [PASS](https://github.com/Kairos-cryptoAI/kairos-aggregator/actions/runs/34699938143) |
| Macro | `b4c82240f76e0ede6ca5943b5509c4c5774c67bb` | 78 | [PASS](https://github.com/Kairos-cryptoAI/kairos-macro-strategist/actions/runs/34699935027) |

All rows also passed locked install, lint, format, type/security checks and
wheel/source build. The Strategy source/test trees are byte-identical to the
previous commit. The backtest repository remains clean at `9ed9d6a`; its frozen
lockfiles and pinned Strategy `fb7d406` have not been updated to the new runtime.

Macro's separately opted-in SQL test was rerun against an isolated tmpfs
PostgreSQL with the new installed Git dependencies: **one PASS, zero skips**.
It restores 65 causal facts from 68 synthetic rows, excludes future/foreign
facts and verifies restart equality without model/bus calls. Receipt:
`D:\Kairos\runtime\macro-history-pin-gate-20260912-p730ff1\receipt.json`, SHA-256
`d0c016a33edbbc28b80726cd86a1856faacf29effdac23ae2415f8d82687bf0e`.

Deploy `466387949b7a6c06d60517cc289ba1b36caf374e` synchronizes both source
manifests and both Compose files. Sixty Windows tests, offline preflight, both
remote-source validators, both Compose syntax checks and the complete
[image-build CI](https://github.com/Kairos-cryptoAI/kairos-deploy/actions/runs/34700110429)
passed. No existing runtime container was recreated and no primary migration
was applied. Published source, built image and running deployment are distinct.

An additional audit found that the existing gates do **not** cover actual Risk
evaluation plus PostgreSQL-backed publish/inbox commit and Redis ACK in one
composition. The existing persistence runtime ordering unit test uses mocks;
the old long-gap integration uses an in-memory bus. A new isolated test-only
harness is being prepared for this boundary. It must use actual RiskService,
DurableMessageBus and Execution with a fake venue, not precomputed decisions.
Until it passes, this receipt does not claim complete pipeline validation.

The single runtime continuation has meanwhile **FAILED**, not completed:

- Failure observed at `2026-09-12T14:34:59.489448Z`, phase `DURABLE_PUBLISH`,
  error `TimeoutError`; last progress `14:33:47.908301Z`.
- Client-confirmed increment: 5,575 bars. Uncertain ETH minute:
  `[1788780000000, 1788780060000)`; outcome remains `UNKNOWN` until a new
  authoritative check. The previously resolved ETH minute is a different event.
- Earlier `RUNNING`, +796 and +5,572 observations are retained historical facts,
  not the current process state. No second automatic continuation was started.
- Automation now follows this failure and fresh diagnostic receipts. It cannot
  reset cursors/queues, migrate/recreate primary services or blindly retry.

Technical tests remain synthetic; actual DEV qualification and elapsed uptime
are unproven. The alternative simulator still needs its durable runtime and
position lifecycle. No alpha approval, live authority or new paid calls follow
from these engineering changes.

## Second incident resolved without replay — 14:47–14:49 UTC

The new uncertain ETH minute is also **COMMITTED_NOT_YET_DISPATCHED**, exactly
one audit/outbox pair with validated contract and canonical hashes. Actual
continuation increment is 5,576, not the 5,575 client-confirmed count. Retained
five-symbol prefixes have no internal gaps or duplicates; the fixed-end
remainder is 76,080 bars. All 5,576 new outbox rows still await dispatch behind
older ordered backlog. This is not completed recovery.

Audit/outbox totals match at 132,195. Orders/trades/cursors/paid rows remain zero,
and the primary database remains on twelve migrations. The exact producer PID
and container are absent. One older outbox lease has expired; it was not cleared.
No current advisory locks or other active transactions were observed.

New sanitized receipts under `D:\Kairos\runtime\gap-diagnostic-20260912T144509Z`:

- `receipt.json`, SHA-256
  `509cc16060673fad73ba08ad828a03baa2acf4702c7e76c5b5652b4328277ef0`;
- `followup-receipt.json`, SHA-256
  `12297b200cae04e47574fcc552bd9aa6430bf07bbe62d7f938ebaf80774c3d41`.

Redis logged five AOF-fsync-delay warnings during 14:29:50–14:32:20 and a
56-second background snapshot during 14:34:12–14:35:08. Later PostgreSQL/Redis
probes were fast. PostgreSQL's long checkpoint write phase may be paced and is
not by itself proof of a blocked commit. The exact timed-out sub-operation and
host culprit are still unproven.

A post-incident hardware snapshot identifies D/E as Seagate ST4000DM004 SATA
and C as WD_BLACK SN7100 NVMe. The Seagate model is specified as a 5,400-RPM
SMR HDD in its [official datasheet](https://www.seagate.com/content/dam/seagate/en_em/content-fragments/products/datasheets/barracuda-3-5-hdd/barracuda-3-5-hdd-DS17-1-2501US-en_EM.pdf).
Docker's retained data image is under `D:\Docker\WSL`. This makes storage
placement a concrete hypothesis to investigate, not a proven attribution of
every delay. No disk image has been moved, no durability setting weakened and
no further data-repair process started. Any relocation must preserve volumes,
backups and a rollback path; raw image replacement or WSL unregister is not an
acceptable shortcut.

## Verified storage backups and isolated composition — 15:04–15:35 UTC

The user approved moving Docker data only to `C:\Kairos\DockerData`; repositories
and research archives remain in `D:\Kairos`. Fresh primary and shadow logical
backups passed isolated restore drills, preserving twelve migrations, eighteen
critical tables and all row/sequence checkpoints. Primary retains 132,195 audit
and outbox rows; shadow retains twenty usage reservations. Temporary test
databases were separately dumped before their tmpfs server stopped.

Seven scoped containers were stopped gracefully, followed by Docker Desktop's
official stop command without force. A new cold backup preserves the actual
data VHD, system VHD and settings file. A second verification held all originals,
copies and the manifest exclusively at once, rehashed both sides and checked
Docker/WSL remained stopped. It passed at `15:21:37 UTC`:
`VERIFIED_SIMULTANEOUS_EXCLUSIVE_COLD_SNAPSHOT`.

Cold-backup directory:
`C:\Kairos\DockerMigrationBackups\migration-20260912T151000Z-5fb3c091`.
The atomic verification receipt SHA-256 is
`16924be260a7b3004f85bba7ae53fc01c2a8e06828bf8dd9c3d572ce71a814ab`.
No original or old backup was deleted/replaced during backup preparation. At
this checkpoint relocation was **not yet complete**: the active setting remained
`D:\Docker\WSL`, the SSD target was empty, and the official settings step plus
post-move data validation were pending. The later completion is recorded below.
No further runtime catch-up was started.

Without starting local Docker, deploy `44be2e2` completed the separate hosted
[actual Risk/Redis/PostgreSQL/Execution gate](RELEASE-GATE-2026-09-12.md):
83 tests passed, and the standard twenty-job deploy CI also passed. This closes
the specifically identified real-Risk composition gap above, not the wider
pipeline, venue qualification, SIM runtime or strategy gates. The primary
databases, frozen research and provider budgets were not changed by that test.

## Official SSD relocation and post-move validation — 15:57–16:11 UTC

After the user opened Docker Desktop, its official Resources / Advanced flow
selected `C:\Kairos\DockerData`; Docker appended `DockerDesktopWSL`.
Apply & restart and Move disk image were confirmed. By 16:02:25 UTC the UI
returned to Engine running. Both VHDs now reside under
`C:\Kairos\DockerData\DockerDesktopWSL`; the old exact D: VHD paths are absent.
The registered `docker-desktop` BasePath points to the new C: `main` directory.
No raw VHD replacement, private-settings write, WSL unregister, factory reset,
volume deletion or repository/archive relocation was used.

A fresh **all-engine** baseline extends the earlier scoped inventory: all
20 container IDs/mounts/restart policies, 24 volumes and 45 existing image IDs
match after relocation. Container running state is deliberately not an identity
comparison. The pre-move receipt SHA-256 is
`3cbc75c856a409379d8a7449ee350786aad90e6f07013d5bd335302ca613f59b`.
The comparison receipt SHA-256 is
`cc43ad2cac131484ccf04293b5a3f6ccbc1869315604066b9568e2ac1aeb26a7`.

**Settings-reader caveat:** the Windows file reader still sees the older
300-byte settings file with D: root (SHA-256
`df38f66fb28ca067bcb11fa2f4853c47db9651c3d03f5e21707d565db394551a`).
Reading the same nominal host path through running Docker WSL gives the current
325-byte file with C: root (SHA-256
`8b6dc09089335568c137e19838c9d25ed28ab1072409f7c25c1d0cd4c969538d`).
The UI, WSL registration and current host-file view agree on C:. The comparison
receipt retains its sole `configured_data_root_not_D_to_C` diagnostic; it was
not rewritten to hide this discrepancy. Do not infer rollback or recreate D:
storage from the older Windows-reader value alone. The cause of the two views
has not been established; no settings file was manually corrected.

Only the two original PostgreSQL containers were started for read-only checks.
Both retain migrations 001–012, all nineteen table-count checkpoints and the
public execution sequence checkpoint. Primary audit/outbox are 132,195 each,
inbox is 12,357. Shadow retains all twenty usage reservations with exact grouped
status, reserved and actual totals unchanged. Database validation is
`PASS_DATABASE_CHECKPOINTS`, receipt SHA-256
`57f16c771d10163cb68c18b3a09fef1e3f91abf6916e687521a1349cc3ebf2a9`.
This is checkpoint validation, not a byte-for-byte proof of every database row.
Both mounted PostgreSQL password files are readable; all thirteen previously
recorded secret files remain readable with unchanged sizes, without printing
their values. The two PostgreSQL containers were cleanly stopped afterwards;
no containers remain running. Cold/logical backups and all diagnostic receipts
are retained in the existing protected backup directory.

Storage relocation is complete. Runtime catch-up, the full local release gate,
SIM runtime, venue qualification and independent strategy evidence remain
separate unfinished work. No paid API, trading/canary, PAPER or LIVE was started.

## Routed Binance transport and installed-image check — 16:22–16:33 UTC

Quant `2754f1ee2084cef0bcce4349896f6ee5f2085c6d` separates the former mixed
connection into `/public/stream` (five `depth10@100ms` subscriptions) and
`/market/stream` (five each of `markPrice@1s`, `kline_1m`, `forceOrder`). This
follows the current official [routing notice](https://developers.binance.com/en/docs/products/derivatives-trading-usds-futures/websocket-market-streams/Important-WebSocket-Change-Notice)
and [connection documentation](https://developers.binance.com/en/docs/products/derivatives-trading-usds-futures/websocket-market-streams/Connect).
The documented legacy unrouted endpoint retains public-stream compatibility;
that is not a valid mixed route for the market streams used by Kairos.

Each worker reconnects independently. Market REST reconciliation cannot block
public depth consumption. Public disconnect/cancellation invalidates its book
**before awaiting the socket close handshake**, retaining the update-ID high
water mark so a replay cannot restore freshness. An exact stream/route allow-list
rejects cross-route payloads, diff-depth and symbol conflicts. Legacy `/stream`
configuration is normalized locally; no legacy network fallback is used.
Authoritative closed-bar finality and frozen research code remain unchanged.

The complete Windows Python 3.11.15 gate passed: **235 tests**, Ruff/format,
mypy, Bandit, locked dependency verification and wheel/source build. Two new
slow-close tests first reproduced stale freshness, then passed after the inner
cleanup fix. Independent static review found no blocking regression. All three
[Quant CI jobs](https://github.com/Kairos-cryptoAI/kairos-quant-scouts/actions/runs/34705353266)
passed (Linux 3.11/3.14 and Windows 3.11).

One bounded **30-second free, read-only transport probe** ran from
`16:22:13.912364` to `16:22:43.927592 UTC` using the modified collector. It observed
depth, mark-price and minute-kline streams for every required symbol. All five
books/funding observations were fresh before cancellation; all books were
invalid after cancellation. Kline observations in this interval do not prove
closed-bar promotion. No force-order event was observed; that sampled stream is
not a complete liquidation tape. REST, paid-provider and trade-call counts were
all zero. This is **PASS_TRANSPORT_ONLY**, not REST/finality, gap-recovery,
lossless-depth, 24-hour availability or venue qualification evidence.

Sanitized receipt:
`D:\Kairos\runtime\binance-split-probe-28ab7cd564e7471ea5e546360fe0ef62.json`,
SHA-256 `d35f842582c47dc35c8de201f74a553b4e075d7c8d7809c8163205d53e3069d4`.

Deploy `bd99d6612bce2e2520082889c4d4677da17f5d0b` updates only the Quant SHA in
the two runtime source manifests and Compose contexts/labels. No backtest pin or
lockfile changed. Both remote source validators, 60 deployment tests, offline
recovery preflight tests and actual rendered base/LIVE/PAPER topology validators
passed locally. An initial unauthenticated remote PAPER verification returned
HTTP errors; the normal authenticated GitHub CLI credential resolved that check
without changing source pins or weakening verification.
All twenty [deployment CI jobs](https://github.com/Kairos-cryptoAI/kairos-deploy/actions/runs/34705538577)
also passed for that exact revision.

The new local runtime image was built on the relocated Docker engine:
`sha256:eb57fc14762563efa1dd4b9ba76ec2761f8262e73759ee13cf1e4e5cc240f799`.
Its network-disabled, read-only, non-root smoke check verified the embedded
source identity, installed `site-packages` imports, configuration and exact
five/public plus fifteen/market partition. The two installed routing files'
SHA-256 hashes match the committed Git bytes, not merely the OCI revision label:

- `binance_ws.py`: `54cfe887f83c282833129aea66dc02eb2c4a7ff3589efa254eeafcb99238ba93`;
- `stream_routes.py`: `dd2974a679b4af24bc4999c40814a19225e862717ebffacfafa18f84769294d8`.

No service consumers or recovery were started by that image build/smoke check.
Its temporary smoke container was automatically removed; existing data volumes,
secrets and rejected strategy policy were not changed.

## Local composition passed; runtime recovery remains guarded — 16:34 UTC

The unchanged [Risk-to-Execution harness](RELEASE-GATE-2026-09-12.md) passed its
first actual local Docker run after SSD relocation: 83 tests, zero skips,
3.14 seconds, one entry and nine observed commit-before-ACK boundaries.
The synthetic dump/logs were preserved before stopping its three new test
containers; all pre-existing containers remained stopped. This closes the
local post-SSD **composition** gate, not the wider pipeline or venue gates.

Read-only inspection identified two blockers to a small runtime catch-up probe:

1. `--maximum-bars` is a limit on **all required bars to the fixed deadline**,
   not a per-invocation progress limit. With 76,080 bars outstanding, requesting
   a 400-bar budget rejects the invocation rather than producing a clean small
   checkpoint. A separate clean-stop limit is needed; a forced kill would again
   risk an unknown publication outcome.
2. The newly pinned Persistence `730ff1a` contains migrations 001–016, while
   the preserved primary has 001–012. `producer_lease` calls `bus.start()`;
   current bus startup migrates and starts the outbox dispatcher **before**
   acquiring the producer lease and validating anchors/the recovery budget.
   Consequently even an eventually rejected small-budget run could migrate the
   primary and begin dispatching its old backlog. Image identity validation
   alone is not a no-DDL/no-dispatch guard.

No such recovery was started. The next operational code change must provide
explicit schema verification without automatic migration, no dispatch during
recovery preflight/append, and a clean per-run progress boundary. It must retain
the same atomic audit/outbox publication and anchor/hash/producer-lease checks.
Fresh backup/integrity checks and a separately controlled delivery phase remain
necessary before continuation. No migration is removed, no queue/cursor is
reset and neither previously uncertain-but-committed bar is replayed.
