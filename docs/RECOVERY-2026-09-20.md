# Runtime recovery observation — 2026-09-20

This is a factual, fail-closed operational receipt.  It records neither
trading performance nor a permission to start a strategy, dispatcher, PAPER, or
LIVE.  Historical receipts and frozen research evidence are not rewritten.

## Observed bounded recovery

The original isolated `kairos-paper-gate` long-gap status file still says
`RUNNING`, but that field is not treated as authoritative by itself.

- At `2026-09-20T17:29:45.0554312+00:00`, the read-only observer found its
  recorded supervisor PID absent.
- The preserved stdout ended with the valid terminal event
  `COMPLETED` at `2026-09-19T16:55:32.9542840Z`.
- The terminal event records all five symbols at the fixed exclusive boundary
  `1789832640000` and exactly `127300` appended bars.
- The source status file remains unchanged, with SHA-256
  `17ea1f9a73709e61020a9dd0988f052edcb879c5f6251337b8299158c65916d5`.
- The latest observer receipt is
  `D:\Kairos\runtime\paper-gate\long-gap-recovery-observation-20260920T173804Z.json`,
  SHA-256 `cfb832ef8c5ba80a1645e73e53900b806e8a9eee5b865e874b7821ad9b5cacd0`.
  It additionally binds the preserved stdout SHA-256
  `4049a8d141fd2200c960ffb165a03524ab80f907e5d053bfd89119c496a7373b`
  and records that the log remained stable while observed.

The derived state is therefore **`COMPLETED_UNVERIFIED`**, not a recovery
acceptance.  It explicitly leaves consumer restart, source-database mutation,
outbox publication, PAPER, and LIVE forbidden.

## Existing post-run evidence

The immediately post-run backup and clone-only recovery preflight from
September 19 remain preserved, but their age prevents them from authorizing a
new operation today:

- The post-recovery clone receipt passed the exact legacy `001`–`012` profile,
  five contiguous Binance bar prefixes, and duplicate/inbox checks.
- At that point, it recorded `259495` audit and outbox rows, no execution
  journal facts, `141879` pending outbox rows, and one expired legacy lease.
- The receipt is
  `D:\Kairos\kairos-deploy\backups\paper-gate-post-recovery-20260919\runtime-recovery-preflight-20260919T170132Z.json`,
  SHA-256 `f19ca19790eb6e0614fed0995e3df2148d5ce2bb80655b145cc90a8443d65126`.

An independent signed, read-only legacy inspection at
`2026-09-20T13:47:59.450429Z` found the exact expired outbox row eligible only
for a disposable clone rehearsal.  Its result is not an apply decision and
does not clear, publish, retry, or modify that row.  The receipt and detached
signature were verified locally with the configured user signing key.

## Required next recovery gate

Before any original-runtime decision, the system still requires a fresh backup
and clone-only continuity/outbox reconciliation.  The eligible legacy row then
requires the separate no-network quarantine clone rehearsal, using the exact
immutable runtime-schema runner image.  The local GitHub token currently lacks
the required `read:packages` scope, so the runner cannot be freshly pulled and
this gate is deliberately not substituted with a local image or tag.

No queue or cursor has been reset.  No dispatcher, trading consumer, exchange
call, paid provider, PAPER session, or LIVE action was started by this
observation.

## Readiness remains unchanged

```text
PAPER_QUALIFIED=false
ALPHA_READY=false
LIVE_READY=false
STRATEGY_POLICY=REJECT_ALL
```
